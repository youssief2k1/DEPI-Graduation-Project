grafana:
 adminPassword: "${GRAFANA_ADMIN_PASSWORD}" 
 service:
  type: LoadBalancer
 persistence:
  enabled: false
prometheus:
 prometheusSpec:
  ServiceMonitorSelectorNiUsesHelmValues: false
 service:
  type: LoadBalancer

alertmanager:
 enabled: false

prometheusOperator:
 resources:
  requests:
   cpu: 100m
   memory: 200Mi
 admissionWebhooks:
  enabled: true